import fs from "node:fs";
import { describe, expect, it } from "vitest";
describe("unified booking form", () => { it("uses editable company defaults in one operational flow", () => { const source = fs.readFileSync("src/app/admin/bookings/new/page.tsx", "utf8"); expect(source).toContain("Escolher o profissional"); expect(source).toContain("/api/v1/settings/scheduling"); expect(source).toContain("config?.defaultUnitId"); expect(source).toContain("config?.defaultServiceId"); expect(source).toContain("config?.defaultProfessionalId"); expect(source).not.toContain("Escolher pelo serviço"); expect(source).not.toContain("bookingMode"); }); });
