import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("WhatsApp webhook engine boundary", () => { it("documents the real default engine migrated to bookings", () => { const route = fs.readFileSync("src/app/api/v1/whatsapp/webhook/route.ts", "utf8"); const assistant = fs.readFileSync("src/modules/assistant/AssistantWhatsApp.service.ts", "utf8"); expect(route).toContain('process.env.WHATSAPP_INBOUND_ENGINE ?? "assistant"'); expect(route).toContain("AssistantWhatsAppService.handleInbound"); expect(assistant).toContain("BookingCommand.service"); }); });
