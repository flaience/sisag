import{describe,expect,it}from"vitest";import{interpretMessage}from"./interpretMessage";describe("deterministic message reference date",()=>{it("resolves today and tomorrow from the supplied instant and timezone",()=>{const instant=new Date("2026-09-24T01:30:00Z");expect(interpretMessage("agendar hoje",instant,"America/Sao_Paulo").slots.dateIso).toBe("2026-09-23");expect(interpretMessage("agendar amanhã",instant,"America/Sao_Paulo").slots.dateIso).toBe("2026-09-24");expect(interpretMessage("agendar hoje",instant,"UTC").slots.dateIso).toBe("2026-09-24")})});
