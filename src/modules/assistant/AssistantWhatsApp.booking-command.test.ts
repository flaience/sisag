import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("production WhatsApp booking path", () => { it("uses bookings availability, explicit confirmation and the unified command", () => { const source = fs.readFileSync("src/modules/assistant/AssistantWhatsApp.service.ts", "utf8"); expect(source).toContain("listServiceLedAvailability"); expect(source).toContain("pendingBookingDraft"); expect(source).toContain("executeBookingCommand"); expect(source).toContain('source: "whatsapp"'); expect(source).not.toContain("AppointmentService.create({"); }); it("uses the tenant-scoped bookings lifecycle for cancel and reschedule", () => { const source = fs.readFileSync("src/modules/assistant/AssistantWhatsApp.service.ts", "utf8"); expect(source).toContain("WhatsAppBookingLifecycleService.listUpcoming"); expect(source).toContain("WhatsAppBookingLifecycleService.cancel"); expect(source).toContain("WhatsAppBookingLifecycleService.reschedule"); expect(source).not.toContain("AppointmentService"); expect(source).not.toContain("AppointmentRepository"); }); });
