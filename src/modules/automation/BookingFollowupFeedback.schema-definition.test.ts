import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("booking follow-up feedback schema", () => { const sql = fs.readFileSync("infra/booking-followup-feedback.sql", "utf8"); it("enforces tenant ownership and one bounded score per booking", () => { expect(sql).toContain("booking_feedbacks_booking_company_fk"); expect(sql).toContain("booking_feedbacks_client_company_fk"); expect(sql).toContain("score between 1 and 5"); expect(sql).toContain("unique (company_id, booking_id)"); }); it("enables row security", () => { expect(sql).toContain("enable row level security"); }); });
