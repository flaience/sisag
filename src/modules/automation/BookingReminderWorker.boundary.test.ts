import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("booking reminder worker boundary", () => { const worker = fs.readFileSync("src/modules/automation/BookingReminderWorker.service.ts", "utf8"); it("claims work safely and recovers stale locks", () => { expect(worker).toContain("for update skip locked"); expect(worker).toContain("status = 'processing' and locked_at <"); expect(worker).toContain("attempts <"); }); it("revalidates tenant, state, phone and time", () => { expect(worker).toContain("eq(bookings.companyId, job.companyId)"); expect(worker).toContain("booking_inactive"); expect(worker).toContain("client_without_phone"); expect(worker).toContain("booking_started"); }); it("publishes and completes atomically with deduplication", () => { expect(worker).toContain("db.transaction"); expect(worker).toContain("onConflictDoNothing"); expect(worker).toContain("outboxId"); }); it("protects the internal trigger", () => { const route = fs.readFileSync("src/app/api/internal/automation/booking-reminders/route.ts", "utf8"); expect(route).toContain("SISAG_INTERNAL_SECRET"); expect(route).toContain("x-sisag-internal-secret"); }); });
