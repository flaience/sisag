import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("assistant reminder response integration", () => { it("checks reminder responses after explicit session workflows and before general interpretation", () => { const source = fs.readFileSync("src/modules/assistant/AssistantWhatsApp.service.ts", "utf8"); const pending = source.indexOf('sessionCtx.pendingIntent === "RESCHEDULE_REQUEST"'); const reminder = source.indexOf("BookingReminderResponseService.handle"); const general = source.indexOf("// 3) interpreta mensagem normalmente"); expect(reminder).toBeGreaterThan(pending); expect(general).toBeGreaterThan(reminder); expect(source).toContain("reminderResponse.handled"); }); });
