import { describe, expect, it } from "vitest"; import { buildRecoveryDraft } from "./BookingRecoveryDraft.service"; describe("assisted recovery draft", () => { it("creates an empathetic draft without promises or autonomous sending", () => { const draft = buildRecoveryDraft({ clientName: "Ana Silva", score: 1, priority: "urgent" }); expect(draft.message).toContain("Olá, Ana"); expect(draft.message).toContain("ouvir você"); expect(draft.rationale).toContain("rapidamente"); expect(draft.message).not.toMatch(/desconto|reembolso|garantimos/i); }); it("supports clients without a name", () => { expect(buildRecoveryDraft({ score: 2, priority: "high" }).message).toContain("Olá, cliente"); }); });
