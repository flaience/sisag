import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("recovery response boundary", () => { const service = fs.readFileSync("src/modules/automation/BookingRecoveryResponse.service.ts", "utf8"); it("requires provider identity and correlates a recent sent contact within tenant", () => { expect(service).toContain("if (!providerMessageId || !message)"); expect(service).toContain('eq(bookingRecoveryDrafts.status, "sent")'); expect(service).toContain("gte(bookingRecoveryDrafts.sentAt, recent)"); expect(service.match(/input\.companyId/g)?.length).toBeGreaterThanOrEqual(7); }); it("deduplicates and preserves human review", () => { expect(service).toContain("onConflictDoNothing"); expect(service).toContain("providerMessageId"); expect(service).toContain("Uma pessoa da nossa equipe"); expect(service).not.toContain("OutboxPublisher"); }); it("records response and explicit alert events", () => { expect(service).toContain('type: "automation.booking_recovery.responded"'); expect(service).toContain('type: "automation.booking_recovery.alert_requested"'); expect(service).toContain("needsAttention"); }); });
