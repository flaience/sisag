import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("scheduling automation secret boundary",()=>{for(const path of ["booking-reminders","booking-followups","booking-recovery-sla"]){it(path+" accepts Docker secret files",()=>{const source=fs.readFileSync("src/app/api/internal/automation/"+path+"/route.ts","utf8");expect(source).toContain('readEnv("SISAG_INTERNAL_SECRET")');expect(source).not.toContain("process.env.SISAG_INTERNAL_SECRET");expect(source).toContain("x-sisag-internal-secret")})}});
