import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("recovery approval UI", () => { it("allows editing and makes sending explicit", () => { const component = fs.readFileSync("src/components/automation/RecoveryDraftReview.tsx", "utf8"); expect(component).toContain("<textarea"); expect(component).toContain("Aprovar e encaminhar"); expect(component).toContain("stale_recovery_draft"); expect(component).toContain('/draft/approve"'); }); });
