import{describe,expect,it}from"vitest";import{recommendRecoveryAction}from"./BookingRecoveryRecommendation.service";describe("recovery agent recommendation",()=>{it("prioritizes explicit human requests",()=>{expect(recommendRecoveryAction({score:2,priority:"high",classification:"human_request",slaEscalated:false,assigned:false})).toMatchObject({suggestedAction:"human_contact",suggestedPriority:"urgent",confidence:98})});it("keeps ambiguous responses under human review",()=>{expect(recommendRecoveryAction({score:2,priority:"high",classification:"other",slaEscalated:true,assigned:true})).toMatchObject({suggestedAction:"review_response",suggestedPriority:"urgent"})});it("never returns an autonomous send action",()=>{for(const classification of [null,"positive","negative","human_request","other"])expect(recommendRecoveryAction({score:1,priority:"urgent",classification,slaEscalated:false,assigned:false}).suggestedAction).not.toMatch(/send|message/)})});
