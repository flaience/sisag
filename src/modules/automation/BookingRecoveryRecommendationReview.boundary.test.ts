import fs from"node:fs";import{describe,expect,it}from"vitest";describe("recommendation review boundary",()=>{const service=fs.readFileSync("src/modules/automation/BookingRecoveryRecommendationReview.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/[id]/recommendation/review/route.ts","utf8");it("protects tenant, version and concurrency",()=>{expect(service.match(/input.companyId/g)?.length).toBeGreaterThanOrEqual(3);expect(service).toContain("stale_recommendation");expect(service).toContain('eq(bookingRecoveryRecommendations.status,"shadow")');expect(service).toContain("concurrent_review")});it("records human review without executing it",()=>{expect(service).toContain("recommendation_reviewed");expect(service).not.toContain("BookingRecoveryManagementService");expect(service).not.toContain("outbox")});it("derives identity from authentication",()=>{expect(route).toContain("authResult.auth.companyId");expect(route).toContain("authResult.auth.userId");expect(route).not.toContain("body.companyId")})});
