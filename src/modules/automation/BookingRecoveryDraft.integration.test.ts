import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("recovery draft UI integration", () => { it("shows generation and explicit non-sending state", () => { const page = fs.readFileSync("src/app/admin/settings/booking-followups/recovery/page.tsx", "utf8"); expect(page).toContain("Preparar abordagem"); expect(page).toContain("Aguardando revisão — nenhuma mensagem foi enviada."); expect(page).toContain('/draft"'); }); });
