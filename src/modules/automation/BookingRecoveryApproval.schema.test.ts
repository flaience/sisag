import { describe, expect, it } from "vitest"; import { BookingRecoveryApprovalSchema } from "./BookingRecoveryApproval.schema"; describe("recovery approval", () => { it("accepts a reviewed message and version", () => { expect(BookingRecoveryApprovalSchema.parse({ version: 2, message: "Olá, Ana. Queremos ouvir você e compreender melhor sua experiência." }).version).toBe(2); }); it("rejects short messages and invalid versions", () => { expect(() => BookingRecoveryApprovalSchema.parse({ version: 0, message: "mensagem curta" })).toThrow(); }); });
