import {describe,expect,it} from "vitest";import{averageDurationMinutes,outcomePercentage}from"./BookingRecoveryOutcomes.service";describe("recovery outcomes",()=>{it("calculates stable rates",()=>{expect(outcomePercentage(3,4)).toBe(75);expect(outcomePercentage(1,3)).toBe(33.3);expect(outcomePercentage(0,0)).toBe(0)});it("calculates rounded durations",()=>{expect(averageDurationMinutes([10,20,31])).toBe(20);expect(averageDurationMinutes([])).toBe(0)})});
