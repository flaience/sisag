import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("assistant follow-up feedback integration", () => { it("checks feedback after explicit sessions and before other generic automation responses", () => { const source = fs.readFileSync("src/modules/assistant/AssistantWhatsApp.service.ts", "utf8"); const pending = source.indexOf('sessionCtx.pendingIntent === "RESCHEDULE_REQUEST"'); const feedback = source.indexOf("BookingFollowupFeedbackService.handle"); const reminder = source.indexOf("BookingReminderResponseService.handle"); expect(feedback).toBeGreaterThan(pending); expect(reminder).toBeGreaterThan(feedback); expect(source).toContain("followupFeedback.handled"); }); });
