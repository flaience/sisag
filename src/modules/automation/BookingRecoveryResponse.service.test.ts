import { describe, expect, it } from "vitest"; import { classifyRecoveryResponse } from "./BookingRecoveryResponse.service"; describe("recovery response classification", () => { it("recognizes only explicit signals", () => { expect(classifyRecoveryResponse("Quero falar com uma pessoa")).toBe("human_request"); expect(classifyRecoveryResponse("Foi péssimo e não resolveu")).toBe("negative"); expect(classifyRecoveryResponse("Obrigado, tudo certo")).toBe("positive"); expect(classifyRecoveryResponse("Pode me explicar melhor?")).toBe("other"); }); });
