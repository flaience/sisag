import fs from"node:fs";import{describe,expect,it}from"vitest";describe("agent outcomes boundary",()=>{const s=fs.readFileSync("src/modules/automation/BookingRecoveryAgentOutcomes.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/route.ts","utf8");it("scopes and bounds reads",()=>{expect(s).toContain("input.companyId");expect(s).toContain("Math.min(Math.max");expect(s).toContain(".limit(5000)");expect(r).toContain("authResult.auth.companyId")});it("is read only",()=>{expect(s).not.toContain(".insert(");expect(s).not.toContain(".update(");expect(s).not.toContain("message:")})});
