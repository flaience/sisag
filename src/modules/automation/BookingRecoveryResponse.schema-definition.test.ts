import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("recovery response schema", () => { const schema = fs.readFileSync("src/drizzle/schema.ts", "utf8"); const sql = fs.readFileSync("infra/recovery-response-lifecycle.sql", "utf8"); it("stores tenant-scoped idempotent responses with RLS", () => { expect(schema).toContain("bookingRecoveryResponses"); expect(schema).toContain("booking_recovery_responses_company_provider_uq"); expect(schema).toContain("classificationCheck"); expect(sql).toContain("enable row level security"); }); });
