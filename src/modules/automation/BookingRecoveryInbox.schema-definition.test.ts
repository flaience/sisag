import fs from "node:fs"; import { describe,expect,it } from "vitest"; describe("recovery inbox schema",()=>{ const schema=fs.readFileSync("src/drizzle/schema.ts","utf8"); const sql=fs.readFileSync("infra/recovery-response-inbox.sql","utf8"); it("tracks acknowledgement and pending lookup",()=>{expect(schema).toContain('acknowledgedBy: uuid("acknowledged_by")');expect(schema).toContain('acknowledgedAt: timestamp("acknowledged_at"');expect(sql).toContain("booking_recovery_responses_company_attention_idx")}); });
