import fs from"node:fs";import{describe,expect,it}from"vitest";describe("recovery recommendation boundary",()=>{const service=fs.readFileSync("src/modules/automation/BookingRecoveryRecommendation.service.ts","utf8"),schema=fs.readFileSync("src/drizzle/schema.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/[id]/recommendation/route.ts","utf8");it("persists versioned shadow decisions",()=>{expect(schema).toContain("booking_recovery_recommendations");expect(service).toContain('status:"shadow"');expect(service).toContain("version:sql");expect(service).toContain("signals")});it("derives tenant from authenticated context",()=>{expect(route).toContain("authResult.auth.companyId");expect(route).not.toContain("body.companyId");expect(service.match(/input.companyId/g)?.length).toBeGreaterThanOrEqual(4)});it("has no execution or sending capability",()=>{expect(service).not.toContain("outbox");expect(service).not.toContain("BookingRecoveryManagementService")})});
