import { describe, expect, it } from "vitest";
import { REMINDER_MAX_ATTEMPTS, reminderOutboxDedupeKey, reminderRetryDelayMinutes, renderBookingReminder } from "./BookingReminderWorker.service";
describe("booking reminder worker", () => { it("uses bounded exponential retry", () => { expect(REMINDER_MAX_ATTEMPTS).toBe(5); expect([1,2,3,4,5,9].map(reminderRetryDelayMinutes)).toEqual([1,2,4,8,16,60]); }); it("uses a stable outbox identity", () => { expect(reminderOutboxDedupeKey("job-key")).toBe("booking-reminder-send:job-key"); }); it("renders Portuguese confirmation instructions in company timezone", () => { const text = renderBookingReminder({ clientName: "Ana", startTime: new Date("2026-09-10T15:00:00.000Z"), timezone: "America/Sao_Paulo" }); expect(text).toContain("Ana"); expect(text).toContain("SIM"); expect(text).toContain("CANCELAR"); expect(text).toContain("12:00"); }); });
