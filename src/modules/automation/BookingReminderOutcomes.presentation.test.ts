import { describe, expect, it } from "vitest"; import { describeReminderImpact, percentage } from "./BookingReminderOutcomes.presentation";
describe("booking reminder outcomes presentation", () => { it("calculates stable rates", () => { expect(percentage(3, 4)).toBe(75); expect(percentage(0, 0)).toBe(0); }); it("explains early and healthy stages", () => { expect(describeReminderImpact({ sent: 0, responses: 0, completed: 0, noShow: 0 })).toContain("primeiros lembretes"); expect(describeReminderImpact({ sent: 10, responses: 8, completed: 9, noShow: 1 })).toContain("forte participação"); }); });
