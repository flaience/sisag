import fs from "node:fs";import{describe,expect,it}from"vitest";describe("scheduling automation runner",()=>{const worker=fs.readFileSync("src/workers/scheduling-automation-runner.mjs","utf8");const dockerfile=fs.readFileSync("Dockerfile","utf8");const deploy=fs.readFileSync(".github/workflows/deploy.yml","utf8");it("runs all operational queues with bounded timing",()=>{expect(worker).toContain("booking-reminders");expect(worker).toContain("booking-followups");expect(worker).toContain("booking-recovery-sla");expect(worker).toContain("AbortSignal.timeout");expect(worker).toContain("Math.min(Math.max")});it("uses private service discovery and a file secret",()=>{expect(worker).toContain("http://app-frontend:3000");expect(worker).toContain("SISAG_INTERNAL_SECRET_FILE");expect(worker).not.toContain("github")});it("packages and deploys one Swarm replica",()=>{expect(dockerfile).toContain("scheduling-automation-runner.mjs");expect(deploy).toContain("sisag_scheduling-automation-runner");expect(deploy).toContain("sisag_internal_secret_v4");expect(deploy).toContain("--replicas 1")})});
