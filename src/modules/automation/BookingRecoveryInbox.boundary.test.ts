import fs from "node:fs"; import { describe,expect,it } from "vitest"; describe("recovery inbox boundary",()=>{ const service=fs.readFileSync("src/modules/automation/BookingRecoveryInbox.service.ts","utf8"); const route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/responses/[id]/acknowledge/route.ts","utf8"); it("scopes list and acknowledgement to tenant",()=>{expect(service.match(/input\.companyId/g)?.length).toBeGreaterThanOrEqual(7);expect(route).toContain("authResult.auth.companyId");expect(route).not.toContain("body.companyId")}); it("acknowledges once and assigns authenticated operator",()=>{expect(service).toContain("isNull(bookingRecoveryResponses.acknowledgedAt)");expect(service).toContain("alreadyAcknowledged");expect(service).toContain("assignedTo: input.actorId");expect(route).toContain("authResult.auth.userId")}); it("audits acknowledgement",()=>expect(service).toContain('type: "automation.booking_recovery.response_acknowledged"')); });
