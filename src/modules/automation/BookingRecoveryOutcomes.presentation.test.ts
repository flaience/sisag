import fs from"node:fs";import{describe,expect,it}from"vitest";describe("recovery outcomes UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/outcomes/page.tsx","utf8");it("shows effectiveness and adjustable sla",()=>{expect(page).toContain("Taxa de resposta");expect(page).toContain("Reconhecimento humano");expect(page).toContain("Taxa de resolução");expect(page).toContain("Fora do SLA");expect(page).toContain("Atender na caixa")})});
