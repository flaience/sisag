import fs from "node:fs"; import { describe,expect,it } from "vitest"; describe("recovery response inbox UI",()=>{ const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/inbox/page.tsx","utf8"); it("shows priority, history and human actions",()=>{expect(page).toContain("Respostas da recuperação");expect(page).toContain("Assumir resposta");expect(page).toContain("Resolver caso");expect(page).toContain("Pedido humano");expect(page).not.toContain("Responder automaticamente")}); });
