import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("assisted recovery draft boundary", () => { const service = fs.readFileSync("src/modules/automation/BookingRecoveryDraft.service.ts", "utf8"); const route = fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/[id]/draft/route.ts", "utf8"); it("derives tenant and context from persisted records", () => { expect(service.match(/input\.companyId/g)?.length).toBeGreaterThanOrEqual(4); expect(service).toContain("bookingRecoveryCases.companyId"); expect(service).toContain("clients.name"); expect(route).toContain("authResult.auth.companyId"); }); it("persists a versioned review draft and audit event", () => { expect(service).toContain('status: "pending_review"'); expect(service).toContain("bookingRecoveryDrafts.version"); expect(service).toContain('type: "automation.booking_recovery.draft_created"'); }); it("has no sending capability", () => { expect(service).not.toContain("outbox"); expect(service).not.toContain("whatsapp.send"); expect(route).not.toContain("approve"); }); });
