export function percentage(part: number, total: number) { return total > 0 ? Math.round((part / total) * 100) : 0; }
export function describeReminderImpact(input: { sent: number; responses: number; completed: number; noShow: number }) { const responseRate = percentage(input.responses, input.sent); const attendanceRate = percentage(input.completed, input.completed + input.noShow); if (!input.sent) return "Os indicadores aparecerão após os primeiros lembretes enviados."; if (responseRate >= 60 && attendanceRate >= 85) return "A automação apresenta forte participação e comparecimento."; if (responseRate < 30) return "Há oportunidade para melhorar mensagem, antecedência ou cadastro de WhatsApp."; if (attendanceRate < 70) return "As respostas estão chegando, mas a taxa de faltas merece atenção."; return "A automação já produz dados úteis para otimizar a agenda."; }
