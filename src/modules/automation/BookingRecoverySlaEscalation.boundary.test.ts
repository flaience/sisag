import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("recovery SLA escalation boundary",()=>{const service=fs.readFileSync("src/modules/automation/BookingRecoverySlaEscalation.service.ts","utf8");const route=fs.readFileSync("src/app/api/internal/automation/booking-recovery-sla/route.ts","utf8");it("is idempotent and tenant scoped",()=>{expect(service).toContain("isNull(bookingRecoveryResponses.slaEscalatedAt)");expect(service.match(/candidate.companyId/g)?.length).toBeGreaterThanOrEqual(3);expect(service).toContain("sla_escalated")});it("does not send customer messages",()=>{expect(service).not.toContain("outbox");expect(service).not.toContain("send")});it("protects and bounds the internal trigger",()=>{expect(route).toContain("SISAG_INTERNAL_SECRET");expect(service).toContain("Math.min(Math.max(input.batchSize")})});
