import fs from "node:fs"; import { describe, expect, it } from "vitest";
describe("booking reminder settings boundary", () => { it("derives company ownership from authentication", () => { const route = fs.readFileSync("src/app/api/v1/settings/booking-reminders/route.ts", "utf8"); expect(route).toContain("requireApiRole"); expect(route).toContain("authResult.auth.companyId"); expect(route).not.toContain("body.companyId"); }); it("exposes the settings in the admin hub", () => { const hub = fs.readFileSync("src/app/admin/settings/page.tsx", "utf8"); expect(hub).toContain("/admin/settings/booking-reminders"); }); });
