import fs from "node:fs"; import { describe, expect, it } from "vitest"; describe("booking recovery management boundary", () => { const service = fs.readFileSync("src/modules/automation/BookingRecoveryManagement.service.ts", "utf8"); const route = fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/[id]/route.ts", "utf8"); it("scopes reads and writes to the authenticated tenant", () => { expect(service.match(/bookingRecoveryCases\.companyId/g)?.length).toBeGreaterThanOrEqual(4); expect(route).toContain("authResult.auth.companyId"); expect(route).not.toContain("body.companyId"); }); it("uses authenticated assignment and optimistic transitions", () => { expect(route).toContain("authResult.auth.userId"); expect(service).toContain("inArray(bookingRecoveryCases.status"); expect(service).toContain("concurrent_recovery_change"); }); it("records operational audit events", () => { expect(service).toContain('type: action === "resolve" || action === "dismiss"'); expect(service).toContain('"automation.booking_recovery.updated"'); expect(service).toContain("actorId: input.actorId"); }); });
