import fs from"node:fs";import{describe,expect,it}from"vitest";describe("knowledge governance boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryKnowledge.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/recovery-knowledge/route.ts","utf8"),t=fs.readFileSync("src/app/api/v1/settings/recovery-knowledge/[id]/status/route.ts","utf8");it("derives tenant and actor from auth",()=>{expect(r).toContain("a.auth.companyId");expect(r).toContain("a.auth.userId");expect(t).toContain("a.auth.companyId")});it("versions immutable drafts and hashes content",()=>{expect(s).toContain("knowledgeHash");expect(s).toContain("version:(previous[0]?.version??0)+1");expect(s).toContain('status:"draft"')});it("permits only guarded transitions with audit",()=>{expect(s).toContain('from=input.action==="approve"?"draft":"approved"');expect(s).toContain("recoveryAgentKnowledgeAudit");expect(s).not.toContain("outbox")})});
