import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval change proposal boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalChangeProposal.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/proposals/route.ts","utf8");it("derives tenant and actor from authentication",()=>{expect(r).toContain("auth.auth.companyId");expect(r).toContain("auth.auth.userId");expect(s).toContain("companyId:input.companyId")});it("compares before persisting",()=>expect(s.indexOf("BaselineComparisonService.compare")).toBeLessThan(s.indexOf(".insert(")));it("stores evidence links and candidate, not document content",()=>{for(const value of ["baselineReportHash","currentReportHash","candidateVersion","comparison:comparison.result"])expect(s).toContain(value);expect(s).not.toContain("documentContent")});it("has no activation or external execution",()=>{for(const value of ["status:\"active\"","activatedAt","outbox","WhatsApp","MCP","automaticPromotion"])expect(s).not.toContain(value)})});
