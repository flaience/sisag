import{describe,expect,it}from"vitest";import{RecoveryRetrievalBaselineCommandSchema as s}from"./RecoveryRetrievalBaseline.schema";describe("retrieval baseline schema",()=>{it("accepts approval with reason",()=>expect(s.safeParse({action:"approve",reason:"baseline revisado",report:{}}).success).toBe(true));it("requires retirement reason",()=>expect(s.safeParse({action:"retire",id:"11111111-1111-4111-8111-111111111111",reason:"x"}).success).toBe(false));it("bounds scope",()=>expect(s.safeParse({action:"approve",scope:"x".repeat(33),reason:"válido",report:{}}).success).toBe(false))});
