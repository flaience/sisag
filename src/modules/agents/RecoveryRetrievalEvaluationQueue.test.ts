import{describe,expect,it,vi}from"vitest";import{buildRecoveryRetrievalEvaluationQueue as build}from"./RecoveryRetrievalEvaluationQueue";const row=(id:string,execution:unknown)=>({id,caseId:`case-${id}`,version:1,createdAt:new Date("2026-09-01T00:00:00Z"),agentExecution:execution});describe("retrieval evaluation queue",()=>{vi.setSystemTime(new Date("2026-09-04T00:00:00Z"));it("prioritizes missing vector labels",()=>{const result=build([row("a",{retrievalShadow:{lexicalTopDocumentIds:["l"],topDocumentIds:["v"]}}),row("b",{retrievalShadow:{lexicalTopDocumentIds:["l"]}})],[]);expect(result[0]?.recommendationId).toBe("a")});it("removes completed and unranked recommendations",()=>expect(build([row("a",{retrievalShadow:{topDocumentIds:["v"]}}),row("b",{})],[{recommendationId:"a",recommendationVersion:1,strategy:"vector",documentId:"v"}])).toEqual([]));it("bounds queue size",()=>expect(build(Array.from({length:60},(_,i)=>row(String(i),{retrievalShadow:{topDocumentIds:["v"]}})),[],100)).toHaveLength(50))});
