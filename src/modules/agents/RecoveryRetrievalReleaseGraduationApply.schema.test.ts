import{describe,expect,it}from"vitest";import{RecoveryRetrievalReleaseGraduationApplySchema as schema}from"./RecoveryRetrievalReleaseGraduationApply.schema";describe("graduation apply schema",()=>{const valid={id:"11111111-1111-4111-8111-111111111111",expectedVersion:2,reason:"Aplicação humana confirmada"};it("accepts a versioned justified command",()=>expect(schema.safeParse(valid).success).toBe(true));it("rejects invalid or expanded authority",()=>{expect(schema.safeParse({...valid,expectedVersion:0}).success).toBe(false);expect(schema.safeParse({...valid,reason:"x"}).success).toBe(false);expect(schema.safeParse({...valid,companyId:"tenant"}).success).toBe(false)})});