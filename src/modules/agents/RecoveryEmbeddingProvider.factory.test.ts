import{afterEach,describe,expect,it,vi}from"vitest";import{createConfiguredRecoveryEmbedding}from"./RecoveryEmbeddingProvider.factory";afterEach(()=>vi.unstubAllEnvs());describe("embedding provider factory",()=>{it("requires explicit complete configuration",()=>{vi.stubEnv("RECOVERY_EMBEDDING_PROVIDER","openai");vi.stubEnv("OPENAI_API_KEY","");vi.stubEnv("OPENAI_EMBEDDING_MODEL","m");expect(createConfiguredRecoveryEmbedding()).toBeUndefined()});it("creates configured OpenAI embeddings",()=>{vi.stubEnv("RECOVERY_EMBEDDING_PROVIDER","openai");vi.stubEnv("OPENAI_API_KEY","secret");vi.stubEnv("OPENAI_EMBEDDING_MODEL","m");expect(createConfiguredRecoveryEmbedding()).toMatchObject({providerName:"openai",timeoutMs:8000})})});
