import{z}from"zod";export const RecoveryKnowledgeCreateSchema=z.object({sourceType:z.string().trim().min(2).max(40),sourceRef:z.string().trim().min(2).max(160),title:z.string().trim().min(3).max(200),content:z.string().trim().min(1).max(2000),validFrom:z.coerce.date().optional(),validUntil:z.coerce.date().nullable().optional()}).strict().refine(x=>!x.validUntil||!x.validFrom||x.validUntil>x.validFrom,{message:"invalid_validity"});export const RecoveryKnowledgeStatusSchema=z.object({action:z.enum(["approve","retire"])}).strict();
