import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval proposal review boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalChangeProposal.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/proposals/route.ts","utf8"),sql=fs.readFileSync("infra/recovery-agent-retrieval-change-proposal-reviews.sql","utf8");it("scopes review by tenant, id and proposed state",()=>{for(const value of ["input.companyId","input.review.id",'status,\"proposed\"'])expect(s).toContain(value)});it("records human reviewer and reason",()=>{for(const value of ["reviewedBy:input.actorId","reviewedAt:new Date()","reviewReason:input.review.reason"])expect(s).toContain(value);expect(r).toContain("auth.auth.userId")});it("allows one terminal review",()=>{expect(s).toContain("proposal_not_reviewable");expect(sql).toContain("'proposed','approved','rejected'")});it("cannot activate or execute candidate",()=>{for(const value of ["activated","outbox","WhatsApp","MCP","automaticPromotion"])expect(s).not.toContain(value)})});
