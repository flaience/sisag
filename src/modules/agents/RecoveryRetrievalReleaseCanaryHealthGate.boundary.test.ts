import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release canary health gate boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseCanaryHealthGate.ts","utf8"),o=fs.readFileSync("src/modules/automation/BookingRecoveryAgentOutcomes.service.ts","utf8");it("uses explicit versioned limits",()=>{for(const value of ["recovery_retrieval_release_canary_health_v1","minimumSelectedExecutions","minimumSuccessRate","maximumFallbackRate","maximumP95DurationMs","maximumAverageTokens","minimumAverageOverlapRate"])expect(s).toContain(value)});it("is observational and requires human review",()=>{expect(s).toContain("automaticAction:false");expect(s).toContain("require human review");for(const value of ["getDb",".insert(",".update(",".delete(","outbox","WhatsApp","provider"])expect(s).not.toContain(value)});it("evaluates persisted per-plan metrics",()=>{expect(o).toContain("evaluateRecoveryRetrievalReleaseCanaryHealth(releaseCanaryMetrics.plans)");expect(o).toContain("health:releaseCanaryHealth")})});
