import{describe,expect,it}from"vitest";import{createRecoveryRetrievalEvidenceReport as report}from"./RecoveryRetrievalEvidenceReport";import{detectRecoveryRetrievalRegression as detect}from"./RecoveryRetrievalRegressionDetector";const make=(score:number)=>report({period:{days:30,from:"a",to:"b"},retrieval:{fallbackRate:1,p95DurationMs:100,averageTokens:10,humanEvaluation:{vector:{relevanceScore:score,precisionAt1:score}},sampling:{maximumDocumentShare:20}},evaluations:[],generatedAt:"now"});describe("retrieval regression detector",()=>{it("detects semantic regression",()=>expect(detect(make(90),make(80))).toMatchObject({status:"regressed",reasons:["vector_relevance","precision_at_1"]}));it("accepts measurable improvement",()=>expect(detect(make(80),make(90)).status).toBe("improved"));it("rejects tampered reports",()=>expect(detect({...make(80),reportHash:"invalid"},make(80)).status).toBe("incomparable"));it("never promotes",()=>expect(detect(make(80),make(80))).toMatchObject({status:"stable",automaticPromotion:false}))});
