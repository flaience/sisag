import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release canary boundary",()=>{const service=fs.readFileSync("src/modules/automation/BookingRecoveryRecommendation.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/[id]/recommendation/route.ts","utf8");it("passes governed release resolution",()=>{expect(route).toContain("Promise.all");expect(route).toContain("experiment,release");expect(service).toContain("releasePolicy")});it("uses vector knowledge only for a selected successful release",()=>{expect(service).toContain('release?.selected&&vectorShadow.mode==="ai"');expect(service).toContain(":knowledge")});it("persists separate normalized release metadata",()=>{expect(service).toContain('replace(/^experiment_/,"release_")');expect(service).toContain("release:{id:release.planId,selected:release.selected}");expect(service).toContain("experiment:undefined")});it("keeps deterministic recommendation authoritative",()=>{expect(service).toContain("const recommendation = recommendRecoveryAction(signals)");expect(service).toContain("...recommendation");expect(service).toContain('engine: "recovery_rules_v1"')});it("has no autonomous operational action",()=>{for(const value of ["outbox","WhatsApp","BookingRecoveryManagementService"])expect(service).not.toContain(value)})});
