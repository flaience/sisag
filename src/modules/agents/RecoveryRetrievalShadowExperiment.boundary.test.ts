import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval shadow experiment boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalShadowExperiment.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/experiments/route.ts","utf8");it("requires an approved proposal in the tenant",()=>{for(const value of ["input.companyId","input.command.proposalId",'status,\"approved\"'])expect(s).toContain(value)});it("allows one active experiment per scope",()=>{expect(s).toContain("active_experiment_exists");expect(s).toContain("input.command.scope")});it("provides a tenant-scoped kill switch",()=>{for(const value of ["active_experiment_not_found","stoppedBy:input.actorId","stopReason:input.command.reason"])expect(s).toContain(value)});it("has no runtime, messaging or promotion capability",()=>{for(const value of ["executeVectorRetrievalShadow","outbox","WhatsApp","MCP","automaticPromotion"])expect(s).not.toContain(value);expect(r).toContain('requireApiRole(request,[\"owner\"])')})});
