import{describe,expect,it}from"vitest";import{createRecoveryRetrievalEvidenceReport as create}from"./RecoveryRetrievalEvidenceReport";const base={period:{days:30,from:"a",to:"b"},retrieval:{qualityGate:{status:"keep_shadow"}},generatedAt:"now"};const a={recommendationId:"r",recommendationVersion:1,documentId:"d",strategy:"vector",rank:1,relevance:"relevant"};describe("retrieval evidence report",()=>{it("is stable regardless of evaluation order",()=>{const x=create({...base,evaluations:[a,{...a,documentId:"e"}]}),y=create({...base,evaluations:[{...a,documentId:"e"},a]});expect(x.reportHash).toBe(y.reportHash);expect(x.evidence.datasetHash).toBe(y.evidence.datasetHash)});it("does not expose raw identifiers",()=>{const report=JSON.stringify(create({...base,evaluations:[a]}));expect(report).not.toContain('"recommendationId"');expect(report).not.toContain('"documentId"');expect(report).toContain("containsPersonalData")});it("changes hash when evidence changes",()=>expect(create({...base,evaluations:[a]}).reportHash).not.toBe(create({...base,evaluations:[{...a,relevance:"irrelevant"}]}).reportHash))});
