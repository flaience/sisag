import fs from"node:fs";import{describe,expect,it}from"vitest";describe("regression detector boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalRegressionDetector.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/regression/route.ts","utf8");it("uses explicit versioned limits",()=>{for(const x of ["maximumRelevanceDrop","maximumPrecisionAt1Drop","maximumFallbackIncrease","maximumP95IncreasePercent","maximumTokenIncreasePercent","maximumConcentrationIncrease","recovery_retrieval_regression_v1"])expect(s).toContain(x)});it("has no IO or promotion",()=>{expect(s).toContain("automaticPromotion:false");for(const x of ["db.insert(","db.update(","db.delete(","outbox"])expect(s).not.toContain(x)});it("bounds authenticated input",()=>{expect(r).toContain("250000");expect(r).toContain('auth.ok===false')})});
