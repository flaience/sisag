import{RecoveryRetrievalStableObservabilityService}from"./RecoveryRetrievalStableObservability.service";import{evaluateRecoveryRetrievalStableHealth,type RecoveryRetrievalStableHealthMetrics}from"./RecoveryRetrievalStableHealthGate";const metrics=(rows:Array<{planId:string;candidateId:string;executions:number;successRate:number;fallbackRate:number;p95DurationMs:number;averageTokens:number}>):RecoveryRetrievalStableHealthMetrics[]=>rows.map(row=>({planId:row.planId,candidateId:row.candidateId,executions:row.executions,successRate:row.successRate,fallbackRate:row.fallbackRate,p95DurationMs:row.p95DurationMs,averageTokens:row.averageTokens}));export class RecoveryRetrievalStableHealthService{static async get(input:{companyId:string;days?:number;now?:Date}){const observation=await RecoveryRetrievalStableObservabilityService.get(input),health=evaluateRecoveryRetrievalStableHealth({current:metrics(observation.current.plans),previous:metrics(observation.previous.plans)});return{period:observation.period,previousPeriod:observation.previousPeriod,health,readOnly:true as const,automaticAction:false as const}}}
