import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release progression review boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseProgressionReview.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-progression-proposals/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-progression-reviews.sql","utf8");it("allows one terminal transition from proposed",()=>{expect(service).toContain('status,\"proposed\"');expect(service).toContain("input.review.decision");expect(service).toContain("release_progression_proposal_not_reviewable")});it("records reviewer, time and reason",()=>{for(const value of ["reviewedBy","reviewedAt","reviewReason"])expect(service).toContain(value)});it("derives tenant and actor from authentication",()=>{expect(service).toContain("companyId,input.companyId");expect(route).toContain("auth.auth.companyId");expect(route).toContain("auth.auth.userId")});it("does not change the plan or execute integrations",()=>{for(const value of ["recoveryAgentRetrievalReleasePlans","rolloutPercent","provider","outbox","WhatsApp","MCP"])expect(service).not.toContain(value);expect(migration).toContain("review_reason")})});
