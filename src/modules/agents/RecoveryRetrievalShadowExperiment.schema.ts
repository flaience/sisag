import{z}from"zod";const reason=z.string().trim().min(3).max(500);export const RecoveryRetrievalShadowExperimentCommandSchema=z.discriminatedUnion("action",[z.object({action:z.literal("create"),proposalId:z.string().uuid(),scope:z.string().trim().min(2).max(32).default("recovery"),sampleRate:z.number().int().min(1).max(100),maxTokensPerRun:z.number().int().min(1).max(10000),maxDurationMs:z.number().int().min(100).max(20000),startsAt:z.coerce.date(),endsAt:z.coerce.date(),reason}),z.object({action:z.literal("stop"),id:z.string().uuid(),reason})]).superRefine((x,ctx)=>{if(x.action==="create"&&x.endsAt<=x.startsAt)ctx.addIssue({code:z.ZodIssueCode.custom,message:"invalid_window"})});export type RecoveryRetrievalShadowExperimentCommand={action:"create";proposalId:string;scope:string;sampleRate:number;maxTokensPerRun:number;maxDurationMs:number;startsAt:Date;endsAt:Date;reason:string}|{action:"stop";id:string;reason:string};
