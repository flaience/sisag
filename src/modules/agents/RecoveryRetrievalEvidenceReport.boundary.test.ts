import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval evidence report boundary",()=>{const core=fs.readFileSync("src/modules/agents/RecoveryRetrievalEvidenceReport.ts","utf8"),service=fs.readFileSync("src/modules/agents/RecoveryRetrievalEvidenceReport.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/evidence-report/route.ts","utf8");it("uses tenant-scoped bounded evidence",()=>{expect(service).toContain("input.companyId");expect(service).toContain(".limit(10000)")});it("excludes raw ids and content",()=>{expect(core).toContain("recommendationFingerprint");expect(core).toContain("documentFingerprint");expect(core).not.toContain("documentContent")});it("is read only",()=>{for(const term of ["db.insert(","db.update(","db.delete(","outbox","WhatsApp"])expect(core+service).not.toContain(term)});it("exports authenticated JSON",()=>{expect(route).toContain("auth.auth.companyId");expect(route).toContain("attachment")})});
