import{z}from"zod";export const RecoveryRetrievalCandidateSchema=z.object({strategy:z.literal("vector"),provider:z.string().trim().min(2).max(64),model:z.string().trim().min(2).max(128),topK:z.number().int().min(1).max(3),maxCandidates:z.number().int().min(3).max(20),minimumSimilarity:z.number().min(0).max(1)}).strict();export const RecoveryRetrievalChangeProposalSchema=z.object({scope:z.string().trim().min(2).max(32).default("recovery"),days:z.number().int().min(1).max(365).default(30),candidateVersion:z.string().trim().min(3).max(64),candidate:RecoveryRetrievalCandidateSchema,reason:z.string().trim().min(3).max(500),baseline:z.unknown()});export type RecoveryRetrievalChangeProposal={scope:string;days:number;candidateVersion:string;candidate:z.infer<typeof RecoveryRetrievalCandidateSchema>;reason:string;baseline:unknown};
