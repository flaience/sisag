import{z}from"zod";export const RecoveryRetrievalBaselineCommandSchema=z.discriminatedUnion("action",[z.object({action:z.literal("approve"),scope:z.string().trim().min(2).max(32).default("recovery"),reason:z.string().trim().min(3).max(500),report:z.unknown()}),z.object({action:z.literal("retire"),id:z.string().uuid(),reason:z.string().trim().min(3).max(500)})]);export type RecoveryRetrievalBaselineCommand=z.infer<typeof RecoveryRetrievalBaselineCommandSchema>;
