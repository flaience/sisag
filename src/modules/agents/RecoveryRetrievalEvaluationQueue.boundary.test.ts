import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval evaluation queue boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalEvaluationQueue.service.ts","utf8"),q=fs.readFileSync("src/modules/agents/RecoveryRetrievalEvaluationQueue.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/retrieval-evaluation-queue/route.ts","utf8");it("bounds tenant reads",()=>{expect(s.match(/input.companyId/g)?.length).toBeGreaterThanOrEqual(2);expect(s).toContain(".limit(500)");expect(s).toContain(".limit(10000)");expect(q).toContain("50")});it("is deterministic and read only",()=>{for(const term of ["insert","update","delete","outbox","WhatsApp"])expect(s+q).not.toContain(term)});it("derives tenant from auth",()=>{expect(r).toContain("auth.auth.companyId");expect(r).not.toContain("body.companyId")})});
