import{z}from"zod";export const RecoveryRetrievalReleaseRollbackReviewSchema=z.object({id:z.string().uuid(),decision:z.enum(["approved","rejected"]),reason:z.string().trim().min(3).max(500)}).strict();export type RecoveryRetrievalReleaseRollbackReview=z.infer<typeof RecoveryRetrievalReleaseRollbackReviewSchema>;
