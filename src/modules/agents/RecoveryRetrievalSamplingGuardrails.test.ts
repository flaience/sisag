import{describe,expect,it}from"vitest";import{evaluateRecoveryRetrievalSample as evaluate}from"./RecoveryRetrievalSamplingGuardrails";const row=(i:number,strategy:string,rank:number,documentId=`d${i%5}`)=>({recommendationId:`r${i%10}`,recommendationVersion:1,strategy,rank,relevance:"relevant",documentId});describe("retrieval sampling guardrails",()=>{it("requires a minimum sample",()=>expect(evaluate([row(1,"vector",1)]).status).toBe("insufficient_data"));it("detects concentration bias",()=>{const rows=Array.from({length:20},(_,i)=>row(i,i%2?"vector":"lexical",i%3+1,"same"));expect(evaluate(rows)).toMatchObject({status:"sample_biased",documents:1,maximumDocumentShare:100})});it("accepts diverse balanced coverage",()=>{const rows=Array.from({length:60},(_,i)=>row(i,i%2?"vector":"lexical",i%3+1,`d${i%10}`));expect(evaluate(rows).status).toBe("representative")})});
