import fs from"node:fs";import{describe,expect,it}from"vitest";describe("governed baseline comparison boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalBaselineComparison.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/regression/route.ts","utf8");it("selects one active baseline inside tenant and scope",()=>{for(const value of ["input.companyId","input.scope",'status,\"active\"',".limit(1)"])expect(s).toContain(value)});it("requires the approved artifact hashes",()=>{expect(s).toContain("baseline_artifact_mismatch");expect(s).toContain("record.reportHash");expect(s).toContain("record.datasetHash")});it("builds current evidence with the authenticated tenant",()=>{expect(s).toContain("RecoveryRetrievalEvidenceReportService.get({companyId:input.companyId");expect(r).toContain("auth.auth.companyId")});it("has no mutation or promotion capability",()=>{for(const value of [".insert(",".update(",".delete(","automaticPromotion:true"])expect(s).not.toContain(value)})});
