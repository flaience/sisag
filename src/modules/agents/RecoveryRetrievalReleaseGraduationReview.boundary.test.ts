import fs from"node:fs";import{describe,expect,it}from"vitest";describe("graduation review boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseGraduationReview.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-graduation-proposals/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-graduation-reviews.sql","utf8");it("protects tenant, pending state, version and concurrency",()=>{for(const value of ["companyId,input.companyId",'status,"proposed"',"version,input.review.expectedVersion","release_graduation_proposal_not_reviewable"])expect(service).toContain(value)});it("records one human terminal review",()=>{for(const value of ["input.review.decision","reviewedBy","reviewedAt","reviewReason","reviewedVersion"])expect(service).toContain(value)});it("derives tenant and actor from owner authentication",()=>{expect(route).toContain('requireApiRole(request,["owner"])');expect(route).toContain("auth.auth.companyId");expect(route).toContain("auth.auth.userId")});it("does not apply graduation or execute integrations",()=>{for(const value of ["releasePlans","stable","provider","outbox","WhatsApp","MCP"])expect(service).not.toContain(value);expect(route).toContain("RecoveryRetrievalReleaseGraduationApplyService.apply");expect(service).not.toContain("RecoveryRetrievalReleaseGraduationApplyService");expect(migration).toContain("reviewed_version")})});
