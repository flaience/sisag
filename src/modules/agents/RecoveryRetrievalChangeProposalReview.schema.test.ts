import{describe,expect,it}from"vitest";import{RecoveryRetrievalChangeProposalReviewSchema}from"./RecoveryRetrievalChangeProposalReview.schema";const valid={id:"11111111-1111-4111-8111-111111111111",decision:"approved",reason:"Evidência revisada pela governança."};describe("retrieval proposal review schema",()=>{it("accepts a human decision",()=>expect(RecoveryRetrievalChangeProposalReviewSchema.parse(valid)).toEqual(valid));it("rejects activation",()=>expect(RecoveryRetrievalChangeProposalReviewSchema.safeParse({...valid,decision:"activated"}).success).toBe(false));it("requires a bounded reason",()=>expect(RecoveryRetrievalChangeProposalReviewSchema.safeParse({...valid,reason:"x"}).success).toBe(false))});
