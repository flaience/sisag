import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval baseline boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalBaseline.service.ts","utf8"),r=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/baselines/route.ts","utf8"),sql=fs.readFileSync("infra/recovery-agent-retrieval-baselines.sql","utf8");it("scopes operations by tenant",()=>{expect(s.match(/input.companyId/g)?.length).toBeGreaterThanOrEqual(4);expect(s).toContain("eq(recoveryAgentRetrievalBaselines.companyId,input.companyId)")});it("validates integrity before transaction",()=>{expect(s.indexOf("detectRecoveryRetrievalRegression")).toBeLessThan(s.indexOf("db.transaction"));expect(s).toContain("invalid_evidence_report")});it("keeps one active baseline",()=>{expect(sql).toContain("WHERE status='active'");expect(s).toContain("superseded_by_new_baseline")});it("is owner only",()=>{expect(r).toContain('["owner"]');expect(r).toContain("auth.auth.companyId");expect(r).toContain("auth.auth.userId")})});
