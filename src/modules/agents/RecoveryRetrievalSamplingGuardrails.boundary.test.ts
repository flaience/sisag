import fs from"node:fs";import{describe,expect,it}from"vitest";describe("sampling guardrails boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalSamplingGuardrails.ts","utf8");it("uses versioned limits",()=>{for(const x of ["minimumEvaluations","minimumRecommendations","minimumDocuments","minimumStrategyBalanceRate","minimumRankCoverageRate","maximumDocumentShare","recovery_retrieval_sample_v1"])expect(s).toContain(x)});it("has no IO or mutations",()=>{for(const x of ["getDb","insert","update","delete","fetch(","outbox"])expect(s).not.toContain(x)})});
