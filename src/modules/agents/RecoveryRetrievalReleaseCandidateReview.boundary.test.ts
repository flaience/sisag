import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release candidate review boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseCandidateReview.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-candidates/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-candidate-reviews.sql","utf8");it("allows only a terminal transition from draft",()=>{expect(service).toContain('status,"draft"');expect(service).toContain("input.review.decision");expect(service).toContain("release_candidate_not_reviewable")});it("records reviewer reason and time",()=>{for(const value of ["reviewedBy","reviewedAt","reviewReason"])expect(service).toContain(value)});it("derives tenant and actor from auth",()=>{expect(route).toContain("auth.auth.companyId");expect(route).toContain("auth.auth.userId");expect(service).toContain("recoveryAgentRetrievalReleaseCandidates.companyId")});it("has no activation or execution capability",()=>{for(const value of ["outbox","whatsapp","provider","runtime","active"])expect(service.toLowerCase()).not.toContain(value);expect(migration).toContain("'draft','approved','rejected'")})});
