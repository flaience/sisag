import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval evaluation metrics boundary",()=>{const s=fs.readFileSync("src/modules/agents/RecoveryRetrievalEvaluationMetrics.ts","utf8"),o=fs.readFileSync("src/modules/automation/BookingRecoveryAgentOutcomes.service.ts","utf8");it("keeps strategies separate",()=>{expect(s).toContain('strategy==="lexical"');expect(s).toContain('strategy==="vector"')});it("uses bounded persisted tenant data",()=>{expect(o).toContain("recoveryAgentRetrievalEvaluations.companyId,input.companyId");expect(o).toContain(".limit(10000)")});it("does not mutate or promote",()=>{for(const term of ["getDb","insert","update","promote","outbox"])expect(s).not.toContain(term)})});
