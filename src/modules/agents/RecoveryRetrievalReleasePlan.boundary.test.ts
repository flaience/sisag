import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release plan boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleasePlan.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-plans/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-plans.sql","utf8");it("requires an approved release candidate",()=>{expect(service).toContain('status,"approved"');expect(service).toContain("approved_release_candidate_not_found")});it("derives tenant scope and identity",()=>{expect(service).toContain("scope:candidate.scope");expect(service.match(/companyId/g)?.length).toBeGreaterThanOrEqual(8);expect(route).toContain("auth.auth.userId")});it("has bounded rollout and one scheduled plan",()=>{expect(migration).toContain("BETWEEN 1 AND 25");expect(migration).toContain("WHERE status='scheduled'");expect(service).toContain("scheduled_release_plan_exists")});it("offers an auditable kill switch",()=>{for(const value of ["stoppedBy","stoppedAt","stopReason"])expect(service).toContain(value)});it("does not execute the release",()=>{for(const value of ["outbox","whatsapp","provider","runtime","retriever"])expect(service.toLowerCase()).not.toContain(value)})});
