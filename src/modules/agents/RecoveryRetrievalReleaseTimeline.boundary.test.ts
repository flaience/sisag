import fs from "node:fs";import{describe,expect,it}from"vitest";describe("retrieval release timeline boundary",()=>{const route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-timeline/route.ts","utf8"),service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseTimeline.service.ts","utf8");it("derives tenant from owner authentication",()=>{expect(route).toContain('requireApiRole(request,["owner"])');expect(route).toContain("auth.auth.companyId");expect(route).not.toContain('searchParams.get("companyId")')});it("is strictly read only",()=>{expect(route).toContain("export async function GET");for(const value of ["export async function POST","export async function PATCH","export async function PUT","getDb","insert(","update(","delete("])expect(service+route).not.toContain(value)});it("does not expose sensitive retrieval content",()=>{for(const value of ["document.content","clientPhone","prompt","apiKey","secret"] )expect(service).not.toContain(value)})});
