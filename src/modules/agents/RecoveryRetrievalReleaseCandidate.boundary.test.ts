import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release candidate boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseCandidate.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-candidates/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-candidates.sql","utf8");it("requires adopted evidence and approved proposal",()=>{expect(service).toContain('decision,"adopt"');expect(service).toContain('status,"approved"');expect(service).toContain("evaluation.evidence")});it("derives candidate on the server",()=>{expect(service).toContain("proposal.candidateVersion");expect(service).toContain("proposal.candidate");expect(route).toContain("auth.auth.companyId");expect(route).toContain("auth.auth.userId")});it("isolates every lookup by tenant",()=>expect(service.match(/companyId/g)?.length).toBeGreaterThanOrEqual(8));it("is immutable and disconnected from runtime",()=>{for(const value of [".update(","outbox","whatsapp","provider","runtime"])expect(service.toLowerCase()).not.toContain(value);expect(service).toContain('status:"draft"');expect(migration).toContain("UNIQUE INDEX")})});
