import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release progression proposal boundary",()=>{const service=fs.readFileSync("src/modules/agents/RecoveryRetrievalReleaseProgressionProposal.service.ts","utf8"),route=fs.readFileSync("src/app/api/v1/settings/booking-followups/recovery/agent-outcomes/release-progression-proposals/route.ts","utf8"),migration=fs.readFileSync("infra/recovery-agent-retrieval-release-progression-proposals.sql","utf8");it("requires a scheduled tenant plan and eligible evidence",()=>{for(const value of ['status,\"scheduled\"','decision.status!==\"eligible_for_expansion\"',"companyId:input.companyId","release_progression_evidence_not_found"])expect(service).toContain(value)});it("allows only gradual bounded increases",()=>{expect(service).toContain("plan.rolloutPercent+25");expect(service).toContain("proposedRolloutPercent<=plan.rolloutPercent");expect(migration).toContain("proposed_rollout_percent > current_rollout_percent");expect(migration).toContain("proposed_rollout_percent <= LEAST(current_rollout_percent + 25, 100)")});it("freezes policies, evidence and author",()=>{for(const value of ["healthPolicyVersion","progressionPolicyVersion","evidence:","createdBy:input.actorId"])expect(service).toContain(value);expect(route).toContain("auth.auth.userId")});it("does not alter plans or execute integrations",()=>{for(const value of ["update(recoveryAgentRetrievalReleasePlans","provider","outbox","WhatsApp","MCP"])expect(service).not.toContain(value)})});
