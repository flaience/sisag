import { describe, expect, it } from "vitest"; import { describePeriod, groupAvailability } from "./Schedule.presentation"; const periods = [{ id: "2", unitId: "u", unitName: "Centro", weekday: 1, startTime: "13:00", endTime: "17:00" }, { id: "1", unitId: "u", unitName: "Centro", weekday: 1, startTime: "08:00", endTime: "12:00" }, { id: "3", unitId: "u", unitName: "Sul", weekday: 6, startTime: "09:00", endTime: "11:00" }]; describe("availability presentation", () => { it("groups and sorts periods in business language", () => { const groups = groupAvailability(periods); expect(groups.map((item) => item.label)).toEqual(["Segunda-feira", "Sábado"]); expect(groups[0]?.periods.map((item) => item.id)).toEqual(["1", "2"]); expect(describePeriod(periods[0]!)).toBe("13:00 às 17:00 · Centro"); }); });
