import fs from "node:fs"; import { describe, expect, it } from "vitest"; const schema = fs.readFileSync("src/drizzle/schema.ts", "utf8"); const migration = fs.readFileSync("infra/availability-exceptions-foundation.sql", "utf8"); describe("availability exception foundation", () => { it("models reversible company-owned exceptions", () => { expect(schema).toContain('export const availabilityExceptions = pgTable('); expect(schema).toContain('companyId: uuid("company_id").notNull()'); expect(schema).toContain('revokedAt: timestamp("revoked_at"'); expect(schema).toContain('availability_exceptions_target_check'); expect(schema).toContain('.enableRLS()'); }); it("protects scope, period and active lookups in the database", () => { expect(migration).toContain("foreign key (company_id, professional_id, unit_id)"); expect(migration).toContain("ends_at > starts_at"); expect(migration).toContain("status in ('active', 'revoked')"); expect(migration).toContain("where status = 'active'"); expect(migration).toContain("enable row level security"); }); });
