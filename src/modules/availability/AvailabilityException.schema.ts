import { z } from "zod";
export const AvailabilityExceptionKindSchema = z.enum(["holiday", "closure", "absence", "block"]);
export const AvailabilityExceptionInputSchema = z.object({ kind: AvailabilityExceptionKindSchema, unitId: z.string().uuid().nullable().optional(), professionalId: z.string().uuid().nullable().optional(), startsAt: z.string().datetime({ offset: true }), endsAt: z.string().datetime({ offset: true }), allDay: z.boolean().default(false), reason: z.string().trim().min(2).max(240) }).superRefine((value, context) => { if (new Date(value.endsAt) <= new Date(value.startsAt)) context.addIssue({ code: "custom", path: ["endsAt"], message: "end_must_be_after_start" }); const needsProfessional = value.kind === "absence" || value.kind === "block"; if (needsProfessional !== Boolean(value.professionalId)) context.addIssue({ code: "custom", path: ["professionalId"], message: needsProfessional ? "professional_required" : "professional_not_allowed" }); });
export type AvailabilityExceptionInput = z.infer<typeof AvailabilityExceptionInputSchema>;
export const AvailabilityExceptionFilterSchema = z.object({ status: z.enum(["active", "revoked", "all"]).default("active"), unitId: z.string().uuid().optional(), professionalId: z.string().uuid().optional(), from: z.string().datetime({ offset: true }).optional(), to: z.string().datetime({ offset: true }).optional() }).superRefine((value, context) => { if (value.from && value.to && new Date(value.to) <= new Date(value.from)) context.addIssue({ code: "custom", path: ["to"], message: "to_must_be_after_from" }); });
export type AvailabilityExceptionFilter = z.infer<typeof AvailabilityExceptionFilterSchema>;
