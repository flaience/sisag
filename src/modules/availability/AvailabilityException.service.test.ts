import { describe, expect, it, vi } from "vitest"; import { createAvailabilityException, listAvailabilityExceptions, revokeAvailabilityException } from "./AvailabilityException.service"; const item = { id: "e", kind: "holiday", unitId: null, unitName: null, professionalId: null, professionalName: null, startsAt: new Date(), endsAt: new Date(Date.now() + 1000), allDay: true, reason: "Feriado", status: "active", createdBy: "user-a", revokedAt: null, revokedBy: null, createdAt: new Date() }; describe("availability exception service boundary", () => { it("always forwards the authenticated company and author", async () => { const list = vi.fn().mockResolvedValue([item]); await listAvailabilityExceptions("company-a", { status: "active" }, { list }); expect(list).toHaveBeenCalledWith("company-a", { status: "active" }); const create = vi.fn().mockResolvedValue(item); const input = { kind: "holiday" as const, startsAt: "2026-12-24T00:00:00-03:00", endsAt: "2026-12-25T00:00:00-03:00", allDay: true, reason: "Feriado" }; await createAvailabilityException("company-a", "user-a", input, { create }); expect(create).toHaveBeenCalledWith("company-a", "user-a", input); const revoke = vi.fn().mockResolvedValue({ ...item, status: "revoked" }); await revokeAvailabilityException("company-a", "user-a", "exception-a", { revoke }); expect(revoke).toHaveBeenCalledWith("company-a", "user-a", "exception-a"); }); it("rejects calls without an ownership boundary", async () => { await expect(listAvailabilityExceptions("", { status: "active" }, { list: vi.fn() })).rejects.toThrow("missing_company_id"); }); });
