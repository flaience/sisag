import { describe, expect, it } from "vitest"; import { slotIsBlockedByExceptions, type BlockingException } from "./AvailabilityException.engine"; const slotStart = new Date("2026-12-24T12:00:00Z"); const slotEnd = new Date("2026-12-24T13:00:00Z"); const period = { startsAt: new Date("2026-12-24T00:00:00Z"), endsAt: new Date("2026-12-25T00:00:00Z") }; const blocked = (exceptions: BlockingException[], professionals: string[] = [], units: string[] = []) => slotIsBlockedByExceptions({ slotStart, slotEnd, exceptions, professionalIds: new Set(professionals), unitIds: new Set(units) }); describe("availability exception engine", () => { it("blocks company-wide holidays", () => { expect(blocked([{ ...period, professionalId: null, unitId: null }])).toBe(true); }); it("blocks only matching units and professionals", () => { expect(blocked([{ ...period, professionalId: null, unitId: "unit-a" }], [], ["unit-a"])).toBe(true); expect(blocked([{ ...period, professionalId: null, unitId: "unit-a" }], [], ["unit-b"])).toBe(false); expect(blocked([{ ...period, professionalId: "professional-a", unitId: null }], ["professional-a"])).toBe(true); expect(blocked([{ ...period, professionalId: "professional-a", unitId: null }], ["professional-b"])).toBe(false); }); it("keeps slots outside the exception period", () => { expect(slotIsBlockedByExceptions({ slotStart, slotEnd, exceptions: [{ professionalId: null, unitId: null, startsAt: new Date("2026-12-25T00:00:00Z"), endsAt: new Date("2026-12-26T00:00:00Z") }], professionalIds: new Set(), unitIds: new Set() })).toBe(false); }); });
