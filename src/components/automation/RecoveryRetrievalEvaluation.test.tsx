import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval evaluation UI",()=>{const s=fs.readFileSync("src/components/automation/RecoveryRetrievalEvaluation.tsx","utf8"),parent=fs.readFileSync("src/components/automation/RecoveryRecommendationReview.tsx","utf8");it("compares both rankings",()=>{for(const value of ["Ranking lexical","Ranking vetorial (sombra)","lexical","vector"])expect(s).toContain(value)});it("offers bounded human labels",()=>{for(const value of ["Relevante","Parcial","Irrelevante","partially_relevant"])expect(s).toContain(value)});it("states the authority boundary",()=>expect(s).toContain("não altera a recomendação"));it("is connected after a recommendation exists",()=>{expect(parent).toContain("RecoveryRetrievalEvaluation");expect(parent).toContain("if(!item)return")})});
