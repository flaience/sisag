import fs from"node:fs";import{describe,expect,it}from"vitest";describe("recovery recommendation review UI",()=>{const source=fs.readFileSync("src/components/automation/RecoveryRecommendationReview.tsx","utf8");it("shows explainable guidance",()=>{expect(source).toContain("Confiança");expect(source).toContain("rationale");expect(source).toContain("não executa a ação automaticamente")});it("offers governed human decisions",()=>{expect(source).toContain('review("accepted")');expect(source).toContain('review("adjusted")');expect(source).toContain('review("rejected")');expect(source).toContain("justificativa")})});
