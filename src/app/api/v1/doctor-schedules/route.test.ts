import { describe, expect, it } from "vitest"; import { GET, POST } from "./route"; describe("retired doctor schedules endpoint", () => { it("directs callers to the tenant-safe professional contract", async () => { for (const response of [await GET(), await POST()]) { expect(response.status).toBe(410); expect(await response.json()).toMatchObject({ error: "endpoint_retired", replacement: "/api/v1/professionals/{id}/schedules" }); } }); });
