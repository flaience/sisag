import { describe, expect, it } from "vitest"; import { GET, POST } from "./route"; describe("retired scheduling configuration endpoint", () => { it("never exposes configurations without an authenticated company", async () => { for (const response of [await GET(), await POST()]) { expect(response.status).toBe(410); expect(await response.json()).toEqual({ ok: false, error: "endpoint_retired", replacement: "/api/v1/settings/scheduling" }); } }); });
