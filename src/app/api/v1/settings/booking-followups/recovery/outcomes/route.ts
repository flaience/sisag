import { NextRequest,NextResponse } from "next/server"; import { requireApiRole } from "@/lib/auth/apiAuth"; import { BookingRecoveryOutcomesService } from "@/modules/automation/BookingRecoveryOutcomes.service"; export async function GET(request:NextRequest){const authResult=await requireApiRole(request,["owner","admin","staff"]);if(authResult.ok===false)return authResult.response;const days=Number(request.nextUrl.searchParams.get("days")??30);const slaHours=Number(request.nextUrl.searchParams.get("slaHours")??4);return NextResponse.json(await BookingRecoveryOutcomesService.get({companyId:authResult.auth.companyId,days,slaHours}))}
