import { NextRequest, NextResponse } from "next/server"; import { requireApiRole } from "@/lib/auth/apiAuth"; import { BookingRecoveryDraftService } from "@/modules/automation/BookingRecoveryDraft.service"; type Context = { params: Promise<{ id: string }> }; export async function POST(request: NextRequest, context: Context) { const authResult = await requireApiRole(request, ["owner", "admin", "staff"]); if (authResult.ok === false) return authResult.response; const { id } = await context.params; const result = await BookingRecoveryDraftService.generate({ companyId: authResult.auth.companyId, caseId: id, actorId: authResult.auth.userId }); return NextResponse.json(result, { status: result.ok ? 200 : result.error === "recovery_case_not_found" ? 404 : 409 }); }
