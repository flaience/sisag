import{NextRequest,NextResponse}from"next/server";import{requireApiRole}from"@/lib/auth/apiAuth";import{detectRecoveryRetrievalRegression}from"@/modules/agents/RecoveryRetrievalRegressionDetector";export async function POST(request:NextRequest){const auth=await requireApiRole(request,["owner","admin"]);if(auth.ok===false)return auth.response;const text=await request.text();if(text.length>250000)return NextResponse.json({status:"incomparable",reasons:["payload_too_large"]},{status:413});try{const body=JSON.parse(text);return NextResponse.json(detectRecoveryRetrievalRegression(body.baseline,body.current))}catch{return NextResponse.json({status:"incomparable",reasons:["invalid_json"]},{status:400})}}
