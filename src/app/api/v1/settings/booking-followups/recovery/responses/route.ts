import { NextRequest, NextResponse } from "next/server"; import { requireApiRole } from "@/lib/auth/apiAuth"; import { BookingRecoveryInboxService } from "@/modules/automation/BookingRecoveryInbox.service"; export async function GET(request: NextRequest) { const authResult = await requireApiRole(request, ["owner", "admin", "staff"]); if (authResult.ok === false) return authResult.response; return NextResponse.json(await BookingRecoveryInboxService.list({ companyId: authResult.auth.companyId, filter: request.nextUrl.searchParams.get("filter") ?? "pending" })); }
