import { NextRequest, NextResponse } from "next/server"; import { requireApiRole } from "@/lib/auth/apiAuth"; import { BookingRecoveryInboxService } from "@/modules/automation/BookingRecoveryInbox.service"; type Context = { params: Promise<{ id: string }> }; export async function POST(request: NextRequest, context: Context) { const authResult = await requireApiRole(request, ["owner", "admin", "staff"]); if (authResult.ok === false) return authResult.response; const { id } = await context.params; const result = await BookingRecoveryInboxService.acknowledge({ companyId: authResult.auth.companyId, responseId: id, actorId: authResult.auth.userId }); return NextResponse.json(result, { status: result.ok ? 200 : 404 }); }
