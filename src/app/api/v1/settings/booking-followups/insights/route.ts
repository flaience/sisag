import { NextRequest, NextResponse } from "next/server"; import { requireApiRole } from "@/lib/auth/apiAuth"; import { BookingFollowupInsightsService } from "@/modules/automation/BookingFollowupInsights.service";
export async function GET(request: NextRequest) { const authResult = await requireApiRole(request, ["owner", "admin"]); if (authResult.ok === false) return authResult.response; const days = Number(request.nextUrl.searchParams.get("days") ?? 30); return NextResponse.json({ ok: true, ...(await BookingFollowupInsightsService.get({ companyId: authResult.auth.companyId, days })) }); }
