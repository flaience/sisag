import { beforeEach, describe, expect, it, vi } from "vitest";
const mocks = vi.hoisted(() => ({ auth: vi.fn(), get: vi.fn(), update: vi.fn(), deactivate: vi.fn() }));
vi.mock("@/lib/auth/apiAuth", () => ({ requireApiRole: mocks.auth }));
vi.mock("@/modules/services/ServiceCatalog.service", () => ({ getCompanyService: mocks.get, updateCompanyService: mocks.update, deactivateCompanyService: mocks.deactivate }));
import { DELETE, GET, PUT } from "./route"; const id = "11111111-1111-4111-8111-111111111111"; const context = { params: Promise.resolve({ id }) };
describe("service item API", () => { beforeEach(() => { vi.clearAllMocks(); mocks.auth.mockResolvedValue({ ok: true, auth: { companyId: "company-a", role: "admin" } }); }); it("scopes reads", async () => { mocks.get.mockResolvedValue({ id }); await GET(new Request("https://sisag.test") as never, context); expect(mocks.get).toHaveBeenCalledWith("company-a", id); }); it("scopes updates and deactivation", async () => { mocks.update.mockResolvedValue({ id }); await PUT(new Request("https://sisag.test", { method: "PUT", body: JSON.stringify({ name: "Consulta", durationMinutes: 30, pricingMode: "free" }) }) as never, context); expect(mocks.update).toHaveBeenCalledWith("company-a", id, expect.objectContaining({ price: "0.00" })); mocks.deactivate.mockResolvedValue({ id }); await DELETE(new Request("https://sisag.test", { method: "DELETE" }) as never, context); expect(mocks.deactivate).toHaveBeenCalledWith("company-a", id); }); });
