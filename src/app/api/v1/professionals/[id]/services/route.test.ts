import { describe, expect, it, vi } from "vitest"; const mocks = vi.hoisted(() => ({ auth: vi.fn(), list: vi.fn(), link: vi.fn() })); vi.mock("@/lib/auth/apiAuth", () => ({ requireApiRole: mocks.auth })); vi.mock("@/modules/professionals/ProfessionalService.service", () => ({ ProfessionalServiceError: class extends Error {}, listProfessionalServices: mocks.list, linkProfessionalService: mocks.link })); import { GET, POST } from "./route"; const id = "11111111-1111-4111-8111-111111111111"; const serviceId = "22222222-2222-4222-8222-222222222222"; const context = { params: Promise.resolve({ id }) }; describe("professional services API", () => { it("uses authenticated company", async () => { mocks.auth.mockResolvedValue({ ok: true, auth: { companyId: "company-a", role: "admin" } }); mocks.list.mockResolvedValue([]); await GET(new Request("https://sisag.test") as never, context); expect(mocks.list).toHaveBeenCalledWith("company-a", id); mocks.link.mockResolvedValue({}); await POST(new Request("https://sisag.test", { method: "POST", body: JSON.stringify({ serviceId }) }) as never, context); expect(mocks.link).toHaveBeenCalledWith("company-a", id, { serviceId, durationOverrideMinutes: null, priceOverride: null }); }); });
