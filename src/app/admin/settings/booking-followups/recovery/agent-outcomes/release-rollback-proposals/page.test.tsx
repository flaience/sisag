import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release rollback proposals UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-rollback-proposals/page.tsx","utf8");it("offers only eligible scheduled plans without pending proposal",()=>{for(const value of ['x.status==="rollback_review_required"','x.status==="rollback_review_recommended"','x.status==="scheduled"','x.status==="proposed"','!pending.has(x.id)'])expect(page).toContain(value)});it("enforces gradual reduction",()=>{for(const value of ["rolloutPercent-25","proposed>=selected.rolloutPercent","proposed<minimum","maxLength={500}"])expect(page).toContain(value)});it("shows evidence and review history",()=>{for(const value of ["Histórico e revisão","healthPolicyVersion","rollbackPolicyVersion","reviewReason","reviewedAt"])expect(page).toContain(value)});it("reviews only pending proposals with reason",()=>{for(const value of ['method:"PATCH"','item.status==="proposed"','note.trim().length<3',"Aprovar proposta","Rejeitar proposta"])expect(page).toContain(value)});it("has no application control",()=>{expect(page).toContain("Aprovação é governança, não aplicação");expect(page).toContain("nenhum rollback é automático");for(const value of ['method:"PUT"',"Aplicar rollback","updateRollout"])expect(page).not.toContain(value)})});
