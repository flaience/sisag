import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval change proposals UI",()=>{const s=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/proposals/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("uses the governed proposals API",()=>{expect(s).toContain("/agent-outcomes/proposals");expect(s).toContain("candidateVersion");expect(s).toContain("baseline")});it("bounds artifact and candidate controls",()=>{for(const value of ["MAX_FILE_BYTES=100000","maxLength={500}",'max={3}','max={20}','max={1}'])expect(s).toContain(value)});it("keeps activation unavailable",()=>{expect(s).toContain("não aprova nem ativa");expect(s).not.toContain('action:\"activate\"')});it("is linked from quality hub",()=>expect(hub).toContain("/agent-outcomes/proposals"))});
