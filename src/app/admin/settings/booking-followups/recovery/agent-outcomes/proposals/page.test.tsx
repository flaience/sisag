import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval proposal review UI",()=>{const s=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/proposals/page.tsx","utf8");it("submits terminal human decisions",()=>{expect(s).toContain('method:"PATCH"');expect(s).toContain('review(item,"approved")');expect(s).toContain('review(item,"rejected")')});it("requires review justification",()=>{expect(s).toContain("note.trim().length<3");expect(s).toContain("Justificativa para aprovar");expect(s).toContain("Justificativa para rejeitar")});it("shows decisions only for pending proposals",()=>{expect(s).toContain('item.status==="proposed"');expect(s).toContain("item.reviewReason")});it("keeps approval non-operational",()=>{expect(s).toContain("não ativa a configuração candidata");expect(s).not.toContain('decision:"activated"')})});
