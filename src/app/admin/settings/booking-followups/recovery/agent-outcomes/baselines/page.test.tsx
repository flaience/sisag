import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval baseline admin UI",()=>{const s=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/baselines/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("uses the governed baseline API",()=>{expect(s).toContain("/agent-outcomes/baselines");expect(s).toContain('action:"approve"');expect(s).toContain('action:"retire"')});it("bounds evidence and justification",()=>{expect(s).toContain("MAX_FILE_BYTES=100000");expect(s).toContain("maxLength={500}");expect(s).toContain("reason.trim().length<3")});it("keeps governance explicit",()=>{expect(s).toContain("não promove configurações nem executa ações");expect(s).toContain("Histórico auditável")});it("is linked from quality hub",()=>expect(hub).toContain("/agent-outcomes/baselines"))});
