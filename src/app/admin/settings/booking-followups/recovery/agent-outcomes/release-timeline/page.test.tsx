import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release timeline UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-timeline/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("offers bounded periods and renders normalized events",()=>{for(const value of ["[7,30,90]","candidate_created","health_snapshot","progression_applied","rollback_applied","previousRolloutPercent"])expect(page).toContain(value)});it("states its read-only boundary",()=>{expect(page).toContain("somente leitura");for(const value of ['method:"POST"','method:"PATCH"','method:"PUT"'])expect(page).not.toContain(value)});it("is linked from observability",()=>expect(hub).toContain("/agent-outcomes/release-timeline"))});
