"use client";import Link from"next/link";import{useState}from"react";import{ArrowLeft,Download,GitCompareArrows}from"lucide-react";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{SisagPage,SisagPageHeader}from"@/components/sisag";type Delta={code:string;before:number;after:number;delta:number;regressed:boolean};type Result={status:"improved"|"stable"|"regressed"|"incomparable";policyVersion:string;reasons:string[];deltas?:Delta[]};const MAX_FILE_BYTES=100000,labels:Record<string,string>={vector_relevance:"Relevância vetorial",precision_at_1:"Precisão no primeiro resultado",fallback_rate:"Fallback",p95_duration:"Latência p95",average_tokens:"Consumo médio",document_concentration:"Concentração documental",invalid_report_hash:"Hash inválido",policy_versions_mismatch:"Políticas incompatíveis"};export default function Page(){const[baseline,setBaseline]=useState<unknown>(null),[current,setCurrent]=useState<unknown>(null),[names,setNames]=useState({baseline:"",current:""}),[result,setResult]=useState<Result|null>(null),[error,setError]=useState<string|null>(null),[busy,setBusy]=useState(false);async function select(kind:"baseline"|"current",file?:File){setError(null);setResult(null);if(!file)return;if(file.size>MAX_FILE_BYTES){setError("Cada dossiê deve ter no máximo 100 KB.");return}try{const value=JSON.parse(await file.text());kind==="baseline"?setBaseline(value):setCurrent(value);setNames(x=>({...x,[kind]:file.name}))}catch{setError("Selecione um arquivo JSON válido.")}}async function compare(){if(!baseline||!current)return;setBusy(true);setError(null);try{const response=await fetch("/api/v1/settings/booking-followups/recovery/agent-outcomes/regression",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({baseline,current})}),data=await response.json();setResult(data)}catch{setError("Não foi possível comparar os dossiês.")}finally{setBusy(false)}}return <SisagPage><SisagPageHeader context={<span className="inline-flex items-center gap-2"><GitCompareArrows className="h-4 w-4"/>Inteligência assistida</span>} title="Comparar regressões do retrieval" description="Valide o relatório atual contra um baseline antes de considerar qualquer mudança."/><div className="flex flex-wrap gap-2"><Button asChild variant="ghost" size="sm"><Link href="/admin/settings/booking-followups/recovery/agent-outcomes"><ArrowLeft className="mr-2 h-4 w-4"/>Voltar à qualidade</Link></Button><Button asChild variant="outline" size="sm"><a href="/api/v1/settings/booking-followups/recovery/agent-outcomes/evidence-report?days=30"><Download className="mr-2 h-4 w-4"/>Exportar relatório atual</a></Button></div><Card className="rounded-2xl"><CardHeader><CardTitle className="text-lg">Dossiês JSON</CardTitle></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><FileInput label="Baseline aprovado" name={names.baseline} onFile={f=>void select("baseline",f)}/><FileInput label="Relatório atual" name={names.current} onFile={f=>void select("current",f)}/><Button disabled={!baseline||!current||busy} onClick={()=>void compare()}>{busy?"Comparando...":"Comparar dossiês"}</Button>{error?<p className="text-sm text-rose-700">{error}</p>:null}</CardContent></Card>{result?<Card className={result.status==="regressed"?"rounded-2xl border-rose-300":"rounded-2xl"}><CardHeader><CardTitle className="text-lg">Resultado: {result.status}</CardTitle></CardHeader><CardContent><p className="text-xs text-slate-500">Política {result.policyVersion} · nenhuma promoção é automática.</p>{result.reasons.length?<p className="mt-2 text-sm text-rose-700">{result.reasons.map(x=>labels[x]??x).join(" · ")}</p>:null}<div className="mt-4 grid gap-2 md:grid-cols-2">{result.deltas?.map(item=><div key={item.code} className={item.regressed?"rounded-xl bg-rose-50 p-3":"rounded-xl bg-slate-50 p-3"}><b className="text-sm">{labels[item.code]??item.code}</b><p className="text-xs">{item.before} → {item.after} · Δ {item.delta}</p></div>)}</div></CardContent></Card>:null}</SisagPage>}function FileInput({label,name,onFile}:{label:string;name:string;onFile:(file?:File)=>void}){return <label className="rounded-xl border border-dashed p-4 text-sm"><b>{label}</b><input className="mt-3 block w-full text-xs" type="file" accept="application/json,.json" onChange={e=>onFile(e.target.files?.[0])}/><span className="mt-2 block text-xs text-slate-500">{name||"Nenhum arquivo selecionado"} · máximo 100 KB</span></label>}
