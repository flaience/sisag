import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval regression UI",()=>{const s=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/regression/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("accepts two bounded reports",()=>{expect(s).toContain("MAX_FILE_BYTES=100000");expect(s).toContain('type="file"');expect(s).toContain("Baseline aprovado");expect(s).toContain("Relatório atual")});it("renders states and deltas",()=>{for(const x of ["improved","stable","regressed","incomparable","item.before","item.after","item.delta"])expect(s).toContain(x)});it("keeps authority visible",()=>expect(s).toContain("nenhuma promoção é automática"));it("is linked from observability",()=>expect(hub).toContain("/agent-outcomes/regression"))});
