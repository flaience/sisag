import fs from"node:fs";import{describe,expect,it}from"vitest";describe("governed retrieval regression UI",()=>{const s=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/regression/page.tsx","utf8");it("accepts one bounded governed artifact",()=>{expect(s).toContain("MAX_FILE_BYTES=100000");expect(s).toContain('type=\"file\"');expect(s).toContain("Dossiê da baseline governada")});it("lets the server create current evidence",()=>{expect(s).toContain('days:30,baseline');expect(s).not.toContain('setCurrent')});it("renders states and deltas",()=>{for(const x of ["improved","stable","regressed","incomparable","item.before","item.after","item.delta"])expect(s).toContain(x)});it("keeps authority visible",()=>expect(s).toContain("nenhuma promoção é automática"))});
