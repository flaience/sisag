import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release plans UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-plans/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("selects only approved candidates",()=>expect(page).toContain('x.status==="approved"'));it("reflects bounded rollout and budgets",()=>{for(const value of ['max={25}','max={10000}','max={20000}','maxLength={500}','type="datetime-local"'])expect(page).toContain(value)});it("exposes an explicit kill switch",()=>{expect(page).toContain('action:"stop"');expect(page).toContain("Acionar kill switch");expect(page).toContain("note.trim().length<3")});it("does not claim activation",()=>{expect(page).toContain("Agendar não ativa");expect(page).toContain("não é consumido pelo runtime")});it("is linked from quality hub",()=>expect(hub).toContain("/agent-outcomes/release-plans"))});
