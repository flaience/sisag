import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release progression proposals UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-progression-proposals/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("offers only scheduled eligible plans without a pending proposal",()=>{for(const value of ['x.status===\"eligible_for_expansion\"','x.status===\"scheduled\"','x.status===\"proposed\"','!pending.has(x.id)'])expect(page).toContain(value)});it("enforces a gradual bounded increase",()=>{expect(page).toContain("rolloutPercent+25");expect(page).toContain("proposed<=selected.rolloutPercent");expect(page).toContain("proposed>maximum");expect(page).toContain('maxLength={500}')});it("shows frozen evidence and history",()=>{for(const value of ["Histórico de propostas","healthPolicyVersion","progressionPolicyVersion","item.evidence?.decision","currentRolloutPercent","proposedRolloutPercent"])expect(page).toContain(value)});it("does not claim review, activation or automatic progression",()=>{expect(page).toContain("não modifica o plano nem aumenta o rollout");expect(page).toContain("nenhuma progressão é automática");for(const value of ["PATCH","approveProposal","activateRelease"])expect(page).not.toContain(value)});it("is linked from the observability hub",()=>expect(hub).toContain("/agent-outcomes/release-progression-proposals"))});
