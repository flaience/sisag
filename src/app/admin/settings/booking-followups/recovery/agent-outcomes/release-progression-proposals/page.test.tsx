import fs from"node:fs";import{describe,expect,it}from"vitest";describe("release progression proposals UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-progression-proposals/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("offers only scheduled eligible plans without a pending proposal",()=>{for(const value of ['x.status===\"eligible_for_expansion\"','x.status===\"scheduled\"','x.status===\"proposed\"','!pending.has(x.id)'])expect(page).toContain(value)});it("enforces a gradual bounded increase",()=>{expect(page).toContain("rolloutPercent+25");expect(page).toContain("proposed<=selected.rolloutPercent");expect(page).toContain("proposed>maximum");expect(page).toContain('maxLength={500}')});it("shows frozen evidence and review history",()=>{for(const value of ["Histórico e revisão","healthPolicyVersion","progressionPolicyVersion","item.evidence?.decision","currentRolloutPercent","proposedRolloutPercent","reviewReason","reviewedAt"])expect(page).toContain(value)});it("reviews only pending proposals with a required reason",()=>{expect(page).toContain('method:\"PATCH\"');expect(page).toContain('item.status===\"proposed\"');expect(page).toContain('note.trim().length<3');expect(page).toContain("Aprovar proposta");expect(page).toContain("Rejeitar proposta")});it("does not claim activation or automatic progression",()=>{expect(page).toContain("Aprovação é governança, não aplicação");expect(page).toContain("nenhuma progressão é automática");for(const value of ["applyRollout","activateRelease"])expect(page).not.toContain(value)});it("is linked from the observability hub",()=>expect(hub).toContain("/agent-outcomes/release-progression-proposals"))});
