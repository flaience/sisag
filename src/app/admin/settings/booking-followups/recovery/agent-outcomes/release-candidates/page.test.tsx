import fs from"node:fs";import{describe,expect,it}from"vitest";describe("retrieval release candidates UI",()=>{const page=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/release-candidates/page.tsx","utf8"),hub=fs.readFileSync("src/app/admin/settings/booking-followups/recovery/agent-outcomes/page.tsx","utf8");it("selects only unused adopted evaluations",()=>{expect(page).toContain('x.decision==="adopt"');expect(page).toContain("!used.has(x.id)")});it("sends references, not technical parameters",()=>{expect(page).toContain("JSON.stringify({evaluationId,reason:reason.trim()})");expect(page).not.toContain("JSON.stringify({candidate")});it("shows candidate and evidence snapshots",()=>{for(const value of ["candidateVersion","minimumSimilarity","observations","successRate","fallbackRuns","p95DurationMs"])expect(page).toContain(value)});it("has no activation action",()=>{expect(page).toContain("não ativa nem promove");expect(page).toContain("desconectados do runtime");expect(page).not.toContain("Ativar candidato")});it("is linked from quality hub",()=>expect(hub).toContain("/agent-outcomes/release-candidates"))});
